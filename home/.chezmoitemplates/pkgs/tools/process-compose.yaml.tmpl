name: process-compose
method: binary
github:
  repo: F1bonacc1/process-compose
  release_asset: process-compose_{{ .chezmoi.os }}_{{ .chezmoi.arch }}.tar.gz
  sha256: process-compose_checksums.txt
archive:
  - path: process-compose
