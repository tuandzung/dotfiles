name: duckdb
{{- if eq .chezmoi.os "linux" }}
method: binary
github:
  repo: duckdb/duckdb
  version: latest
  release_asset: duckdb_cli-{{ .chezmoi.os }}-{{ .chezmoi.arch }}.zip
archive:
  - path: duckdb
{{- else }}
method: os
macos:
  packages:
    - name: duckdb
{{- end }}
