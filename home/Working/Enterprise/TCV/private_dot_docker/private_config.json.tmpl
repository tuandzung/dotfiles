{{- if has "TCV" .decryptEnterprise }}
{{-   $top := merge . (dict
        "enterprise" (dict
          "name" "TCV"
          "scope" "global"
        )
      )
}}
{{- includeTemplate "docker/config.yaml.tmpl" $top | fromYaml | toPrettyJson "\t" }}
{{- end -}}
